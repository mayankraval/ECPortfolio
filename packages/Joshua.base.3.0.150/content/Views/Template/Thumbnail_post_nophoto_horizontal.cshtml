@model IEnumerable<Joshua.Models.Content>
@{
  var thumbsize = 3; // from 1 to 12. 
}
@if (Model != null)
{
  foreach (var i in Model)
  { 
  <div class="span@(thumbsize) thumbnail-box">
    <a href="@i.Path">
      <h2>@i.Title</h2>
    </a>
    @MvcHtmlString.Create(i.Abstract)
  </div>
  }
}



